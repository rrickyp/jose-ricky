
time;
temp = srand(time);
temp = time % 10;

if ( 0 <= temp < 3 ){
    int tmp;
    int result
    result = a * b;
    cout << 3 << "*" << 4 << "is: " << endl;
    cin << tmp; //user input
    if(tmp == result){
        return true;//user can continue.
    }
    return false;//user lost the blackjack game.
}
